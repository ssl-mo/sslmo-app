part of 'index.dart';

class SMHeight extends SizedBox {
  SMHeight(
    double height, {
    super.key,
  }) : super(height: height.h);
}

class SMWidth extends SizedBox {
  SMWidth(
    double width, {
    super.key,
  }) : super(width: width.w);
}
